<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
      <h1>Wikis</h1>
      <% @wikis.each do |wiki| %>
      <h3><%= link_to wiki.title, wiki_path(wiki.id) %></h3>
      <% end %>
    </div>
  </div>
</div>
